#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../../../mocha/1.9.0/mocha/bin/mocha" "$@"
  ret=$?
else 
  node  "$basedir/../../../../../mocha/1.9.0/mocha/bin/mocha" "$@"
  ret=$?
fi
exit $ret
